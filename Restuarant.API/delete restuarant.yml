info:
  name: delete restuarant
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/api/restuarant/{{api-version}}/{restuarantId}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
