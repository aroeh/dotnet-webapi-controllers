info:
  name: get restuarant
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/restuarant/{restuarantId}?api-version={{api-version}}"
  params:
    - name: api-version
      value: "{{api-version}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
