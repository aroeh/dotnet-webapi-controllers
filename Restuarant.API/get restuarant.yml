info:
  name: get restuarant
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/restuarant/{restuarantId}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
